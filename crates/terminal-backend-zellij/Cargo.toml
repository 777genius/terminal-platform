[package]
name = "terminal-backend-zellij"
version = "0.1.0"
edition = "2024"

[dependencies]
terminal-backend-api = { path = "../terminal-backend-api" }
terminal-domain = { path = "../terminal-domain" }

[lints]
workspace = true
