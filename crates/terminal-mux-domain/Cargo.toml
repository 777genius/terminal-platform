[package]
name = "terminal-mux-domain"
version = "0.1.0"
edition = "2024"
license.workspace = true

[dependencies]
serde = { workspace = true }
terminal-domain = { path = "../terminal-domain" }

[lints]
workspace = true
